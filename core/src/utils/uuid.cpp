/**
 * @file uuid.cpp
 * @brief UUID implementation (header-only, but we keep cpp for linkage)
 */

#include "uuid.hpp"

// Implementation is in header (inline)
// This file ensures the translation unit exists for the build
